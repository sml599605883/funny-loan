import 'package:flutter/material.dart';

abstract class AppColors {
  static const gradientStart = Color(0xFF8FC4FB);
  static const gradientEnd = Color(0xFF2166CC);

  static const defaultBackgroundGradient = <Color>[
    gradientStart,
    gradientEnd,
  ];
}
